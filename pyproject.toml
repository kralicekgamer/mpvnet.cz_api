[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]

[project]
name = "mpvnet_cz_api"
version = "1.0"
description = "API for mpvnet.cz."
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name="Matěj Adamec", email="kralyk@kralyk.dev" }
]